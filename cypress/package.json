{
  "name": "cypress-tcc",
  "version": "1.0.0",
  "private": true,
  "description": "TCC - Automacao de testes E2E com Cypress (SauceDemo)",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --browser chrome",
    "report:clean": "rimraf cypress/reports",
    "test": "npm run report:clean && cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "^15.16.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "rimraf": "^6.0.1"
  }
}
